name: 'lightdash_demo_saas'
version: '1.7.6'
config-version: 2
profile: 'lightdash_demo_saas'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  lightdash_demo_saas:
    +materialized: table